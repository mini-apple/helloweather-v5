import React from "react";
import SetProfile from "components/profile/SetProfile";

const Online__profile = ({ userObj }) => {
  return (
    <>
      <SetProfile userObj={userObj} />
    </>
  );
};

export default Online__profile;
