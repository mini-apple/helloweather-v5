import React from "react";

function ExplainString() {
  return (
    <ul className="string-ul">
      <li>정답코드는 인도자가 만들어 공유해줍니다.</li>
      <li>지역 순서는 예보게임지의 순서대로 위에서부터 지역1, 지역2입니다.</li>
    </ul>
  );
}

export default ExplainString;
